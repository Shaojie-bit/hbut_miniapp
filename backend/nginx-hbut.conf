server {
    listen 80;
    server_name hbut.zmj888.asia;
    
    # 重定向到 HTTPS（配置 SSL 后启用）
    # return 301 https://$server_name$request_uri;
    
    # 如果暂时不使用 HTTPS，使用以下配置：
    location / {
        proxy_pass http://127.0.0.1:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_connect_timeout 60s;
        proxy_send_timeout 60s;
        proxy_read_timeout 60s;
    }
}

# HTTPS 配置（配置 SSL 证书后使用）
# server {
#     listen 443 ssl http2;
#     server_name hbut.zmj888.asia;
#     
#     # SSL 证书路径（宝塔面板会自动配置）
#     ssl_certificate /www/server/panel/vhost/cert/hbut.zmj888.asia/fullchain.pem;
#     ssl_certificate_key /www/server/panel/vhost/cert/hbut.zmj888.asia/privkey.pem;
#     ssl_protocols TLSv1.2 TLSv1.3;
#     ssl_ciphers HIGH:!aNULL:!MD5;
#     ssl_prefer_server_ciphers on;
#     
#     location / {
#         proxy_pass http://127.0.0.1:8000;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#         proxy_connect_timeout 60s;
#         proxy_send_timeout 60s;
#         proxy_read_timeout 60s;
#     }
# }
