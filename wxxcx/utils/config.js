const config = {
    // Mini Program requires HTTPS
    // The backend is proxied via Nginx at https://hbut.zmj888.asia
    BASE_URL: "https://hbut-backend-whkzceunvo.cn-hangzhou.fcapp.run"
};

module.exports = config;
